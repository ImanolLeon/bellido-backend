# Variables de entorno
```
DB_USERNAME = username
DB_PASSWORD = Password
DB_URL = jdbc:postgresql://localhost:5432/bellido_map
```

En mi caso estoy usando postgres , cambia según gustes la base de datos.
